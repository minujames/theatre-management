  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Theatre Management Admin Page</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item active"><a class="nav-link" href="#">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="#">Add a Movie</a></li>
        <li class="nav-item"><a class="nav-link" href="#">View Schedule</a></li>
      </ul>
    </div>
  </nav><br><br><br>

  <h5>      This is the User Landing Page</h5>
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-1"></div>

      <div class="col-sm-2">
        <div class="row">
          <h2 id="shows"></h2>
        </div>
        <div class="row">
          <ul class="list-group" id="current">
            {{#each movies}}
              <button class="list-group-item" data-value={{moviename}}>{{moviename}}</button>
            {{/each}}
          </ul>
        </div><br>

        {{!-- <div class="row">
          <h2>Coming soon</h2>
          </div>
          <div class="row">
          <ul class="list-group" id="comingsoon">
            {{#each movies}}
              <button class="list-group-item" data-value={{moviename}}>{{moviename}}</button>
            {{/each}}
          </ul>
          </div> --}}

      </div>

      <div class="col-sm-1"></div>

      <div class="col-sm-7">
        <div id="adminLandingShowScreen">
          <div class="row">
            <h1 id="adminLandingScreenMovieTitle"></h1>
          </div><br>

        <div class="col-sm-2">
          <img src="#" id="movieposterhere" height="120">
        </div>

        <div class="col-sm-1"></div>
          <div class="col-sm-6">
        <div id="addmoviesection">
          <h1 id="movietoadd"></h1>
        </div>
      </div>
        <div class="row">
    <div class="col-sm-1"></div>

    <div class="col-sm-4">
      <ul class="list-group" id="movieinfohere">
      </ul>
    </div>

  </div>


          <div class="form-group">
            <label for="startselect">Select Dates:</label>
            <input id="startselect" type="date">
            {{!-- <input id="endselect" type="date"> --}}
          </div>

          <div class="form-check form-check-inline">
            <label class="form-check-label">
              <button type="button" class="btn btn-info btn-lg " id="timeA" disabled="true">9:00AM</button>
              {{!-- <input class="form-check-input" type="checkbox" id="timeA" value="timeA"> 9:00AM --}}
            </label>
          </div>
          <div class="form-check form-check-inline">
            <label class="form-check-label">
              <button type="button" class="btn btn-info btn-lg" id="timeB" disabled="true">12:00AM</button>
              {{!-- <input class="form-check-input" type="checkbox" id="timeB" value="trueB"> 12:00PM --}}
            </label>
          </div>
          <div class="form-check form-check-inline">
            <label class="form-check-label">
              <button type="button" class="btn btn-info bn-lg" id="timeC" disabled="true">6:00AM</button>
              {{!-- <a href="#" class="btn btn-info btn-lg disabled" role="button" aria-disabled="true">6:00AM</a> --}}
              {{!-- <input class="form-check-input" type="checkbox" id="timeC" value="trueC"> 6:00PM --}}
            </label>
          </div>

          
          <div class="reservation">
            <button type="button" class="btn btn-primary btn-lg btn-block" id="remainingSeats">Remaining Seats (Click to reserve)</button>
            <button type="button" class="btn btn-secondary btn-lg btn-block" id="reservedSeats" disabled>Reserved Seats</button>
          </div>



  {{!--           <h1>Screen 1</h1>
          <div id="adminLandingScreen1">
            <table>
              <thead>
                <tr>
                  <th><h3>Start Date |</h3></th>
                  <th><h3>End Date |</h3></th>
                  <th><h3>Time</h3></th>
                </tr>
              </thead>
              <tbody id="tableCurrent">
              </tbody>
            </table>
          </div><br> --}}

    {{!--           <h1>Screen 2</h1>
          <div id="adminLandingScreen2">
            <table>
              <thead>
                <tr>
                  <th><h3>Start Date |</h3></th>
                  <th><h3>End Date |</h3></th>
                  <th><h3>Time</h3></th>
                </tr>
              </thead>
              <tbody id="tableComingSoon">
              </tbody>
            </table>
          </div> --}}


        </div>
      </div>
    </div>
  </div>

  <script>

  // $("#getinfobutton").on("click", function(event) {
  //   event.preventDefault();

  // })
    // var today = moment().format('YYYY-MM-DD');
    var schedule = [
    //first object is for testing purpose
    {
      title: "Thor",
      date : moment().format('YYYY-MM-DD'),
      showTime: "timeA"
    }, {
      title: "Moana",
      date : "2017-11-06",
      showTime: ["timeB", "timeC"]
    }, {
      title: "Max Payne",
      date : "2017-11-07",
      showTime: "timeC"
    },
   {
        "date": "2017-11-15",
        "Movie.id": 1,
        "Movie.title": "frozen",
        "Movie.releaseYear": 2013,
        "Screen.id": 1,
        "Screen.name": "screen-1",
        "Screen.seats": 10,
        "ShowTime.id": 1,
        "ShowTime.name": "9.00 am"
    },
    {
        "date": "2017-11-15",
        "Movie.id": 1,
        "Movie.title": "frozen",
        "Movie.releaseYear": 2013,
        "Screen.id": 2,
        "Screen.name": "screen-2",
        "Screen.seats": 15,
        "ShowTime.id": 1,
        "ShowTime.name": "9.00 am"
    }
]



    // console.log(schedule[0].title);


  // $(document).ready(function() {
  //   $("#startselect").datepicker();
  // });

    // var current = ["Thor", "Moana", "lion king", "max payne", "star wars"];
    var current = [];
    for (var i = 0; i < schedule.length; i++) {
      current.push(schedule[i].title);
    }
      
    $("#shows").append("Today Shows")
    current.forEach(function(e) {
      var temp = $("<button>").attr("class", "list-group-item").attr("data-value", e).text(e);
      $("#current").append(temp);
    })



    // var comingsoon = ["comingsoon1", "comingsoon2", "comingsoon3", "comingsoon4", "comingsoon5"];
    // comingsoon.forEach(function(e) {
    //   var temp = $("<button>").attr("class", "list-group-item").attr("data-value", e).text(e);
    //   $("#comingsoon").append(temp);
    // })
    

    $(".list-group-item").on("click", function(event) {
      event.preventDefault();
      $("#tableCurrent").empty();
      $("#tableComingSoon").empty();
      var title = $("<p>").html($(this).attr("data-value")).attr("class", "animated flash")
      $("#adminLandingScreenMovieTitle").html(title)

      var name = $(this).attr("data-value")

      // var title = $("<p>").html("Click a Movie Title!").attr("class", "animated flash")
      // $("#adminLandingScreenMovieTitle").html(title)

    $("#movieinfohere").empty();
    // var movie = $("#movietitle").val().trim();
    // $("#movietitle").val("")

      console.log(name);
      console.log($(this).attr("data-value"));        
      $.ajax({
      url: "https://www.omdbapi.com/?t=" + name + "&y=&plot&apikey=40e9cece",
      method: "GET"
    }).done(function(response) {
      $("#movieposterhere").attr("src", response.Poster);
      var movietitle = $("<li>").html("<h4>Title: " + response.Title + "</h4>");
      console.log("I am running");
      // var release = $("<li>").html("<h4>Release Date: " + response.Released + ", Rating: " + response.Rated + "</h4>");
      var genre = $("<li>").html("<h4>Genre: " + response.Genre);
      // var actors = $("<li>").html("<h4>Actors: " + response.Actors + "</h4>");
      // var plot = $("<li>").html("<h4>Plot: " + response.Plot + "</h4>");
      $("#movieinfohere").append(movietitle,  genre);
        // , release, actors, plot);
      // $("#movietoadd").text("Adding " + response.Title).attr("class", "animated flash");
      // $("#addbutton").show();
      })  
    })

  </script>